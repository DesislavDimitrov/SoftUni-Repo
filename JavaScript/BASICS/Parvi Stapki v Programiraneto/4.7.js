function solve(name, project) {
    let time=3;

    let output=`The architect ${name} will need ${time * project} hours to complete ${project} project/s.`


    console.log(output)
    
}

solve("George", 4);
solve("Sanya", 9);